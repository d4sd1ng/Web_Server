services:
  traefik:
    image: traefik:v3.6
    container_name: traefik
    restart: unless-stopped
    mem_limit: 512m
    cpus: "1.00"
    env_file:
      - /opt/server/.env
    environment:
      - CF_API_EMAIL=${CF_API_EMAIL}
      - CF_DNS_API_TOKEN=${CLOUDFLARE_ZONE_API_TOKEN}
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /opt/server/traefik/traefik.yml:/traefik.yml:ro
      - /opt/server/traefik/config:/config:ro
      - /opt/server/traefik/letsencrypt:/letsencrypt
      - /opt/server/logs/traefik/logs:/logs
    networks:
      - proxy
    command:
      - --configfile=traefik.yml
    labels:
      - traefik.enable=true
      - traefik.http.routers.traefik.rule=Host(`${TRAEFIK_HOST}`)
      - traefik.http.routers.traefik.entrypoints=websecure
      - traefik.http.routers.traefik.tls=true
      - traefik.http.routers.traefik.tls.certresolver=cloudflare
      - traefik.http.routers.traefik.service=api@internal
      - traefik.http.routers.traefik.middlewares=default-security@docker,traefik-auth@docker

      - traefik.http.middlewares.traefik-auth.basicauth.users=${TRAEFIK_BASIC_AUTH_USERS}

      - traefik.http.middlewares.default-security.headers.browserXssFilter=true
      - traefik.http.middlewares.default-security.headers.contentTypeNosniff=true
      - traefik.http.middlewares.default-security.headers.forceSTSHeader=true
      - traefik.http.middlewares.default-security.headers.stsIncludeSubdomains=true
      - traefik.http.middlewares.default-security.headers.stsPreload=true
      - traefik.http.middlewares.default-security.headers.stsSeconds=31536000
      - traefik.http.middlewares.default-security.headers.frameDeny=true
      - traefik.http.middlewares.default-security.headers.customFrameOptionsValue=SAMEORIGIN
      - traefik.http.middlewares.default-security.headers.referrerPolicy=no-referrer
      - traefik.http.middlewares.default-security.headers.permissionsPolicy=accelerometer=(),camera=(),geolocation=(),microphone=()
      - traefik.http.middlewares.default-security.headers.customRequestHeaders.X-Forwarded-Proto=https

  adguard:
    image: adguard/adguardhome:latest
    container_name: adguard
    restart: unless-stopped
    mem_limit: 512m
    cpus: "1.00"
    network_mode: host
    volumes:
      - /opt/server/adguard/work:/opt/adguardhome/work
      - /opt/server/adguard/conf:/opt/adguardhome/conf

  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    mem_limit: 512m
    cpus: "1.00"
    environment:
      - DOMAIN=https://${VAULTWARDEN_HOST}
      - SIGNUPS_ALLOWED=false
      - ADMIN_TOKEN=${VAULTWARDEN_ADMIN_TOKEN}
      - WEBSOCKET_ENABLED=true
    volumes:
      - /opt/server/vaultwarden/data:/data
    networks:
      - proxy
      - backend
    labels:
      - traefik.enable=true
      - traefik.http.routers.vaultwarden.rule=Host(`vault.internal.avataryx.de`)
      - traefik.http.routers.vaultwarden.entrypoints=websecure
      - traefik.http.routers.vaultwarden.tls=true
      - traefik.http.routers.vaultwarden.tls.certresolver=cloudflare
      - traefik.http.services.vaultwarden.loadbalancer.server.port=80
      - "traefik.docker.network=server"

  portainer:
    image: portainer/portainer-ce:latest
    container_name: portainer
    restart: unless-stopped
    mem_limit: 512m
    cpus: "1.00"
    command: -H unix:///var/run/docker.sock
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /opt/server/portainer/data:/data
    networks:
      - proxy
      - backend
    labels:
      - traefik.enable=true
      - traefik.http.routers.portainer.rule=Host(`portainer.internal.avataryx.de`)
      - traefik.http.routers.portainer.entrypoints=websecure
      - traefik.http.routers.portainer.tls=true
      - traefik.http.routers.portainer.tls.certresolver=cloudflare
      - traefik.http.services.portainer.loadbalancer.server.port=9000
      - "traefik.docker.network=server"

  homer:
    image: b4bz/homer:latest
    container_name: homer
    restart: unless-stopped
    mem_limit: 256m
    cpus: "0.50"
    user: "1000:1000"
    volumes:
      - /opt/server/homer/assets:/www/assets
    networks:
      - proxy
    labels:
      - traefik.enable=true
      - traefik.http.routers.homer.rule=Host(`homer.internal.avataryx.de`)
      - traefik.http.routers.homer.entrypoints=websecure
      - traefik.http.routers.homer.tls=true
      - traefik.http.routers.homer.tls.certresolver=cloudflare
      - traefik.http.services.homer.loadbalancer.server.port=8080
      - "traefik.docker.network=server"
      - com.centurylinklabs.watchtower.enable=true

  postgres:
    image: postgres:16
    container_name: postgres
    restart: unless-stopped
    mem_limit: 1024m
    cpus: "1.50"
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - /opt/server/postgres/data:/var/lib/postgresql/data
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
  
  watchtower:
    image: containrrr/watchtower:latest
    container_name: watchtower
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - TZ=Europe/Berlin
      - WATCHTOWER_CLEANUP=true
      - WATCHTOWER_INCLUDE_RESTARTING=true
      - DOCKER_API_VERSION=1.54
    command: --label-enable --monitor-only --schedule "0 0 5 * * *"
    
  pgladmin:
    image: dpage/pgadmin4:latest
    container_name: pgladmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@avataryx.de
      PGADMIN_DEFAULT_PASSWORD: ChangeThisPassword
    volumes:
      - /opt/server/pgladmin/data:/var/lib/pgladmin
    networks:
      - proxy
    labels:
       - "traefik.enable=true"
       - "traefik.docker.network=traefik"
       - "traefik.http.routers.pgladmin.rule=Host('pgadmin.internal.avataryx.de'^)"
       - "traefik.http.routers.pgladmin.entrypoints=websecure"
       - "traefik.http.routers.pgladmin.tls=true"
       - "traefik.http.routers.pgladmin.tls.certresolver=cloudflare"
       - "traefik.http.services.pgladmin.loadbalancer.server.port=80"
#       - "traefik.http.routers.pgladmin.middlewares=authelia@docker"

networks:
  proxy:
    external: true
    name: server
  backend:
    name: backend
    driver: bridge
